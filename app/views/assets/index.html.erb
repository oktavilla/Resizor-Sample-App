<h1>Listing assets</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Resizor ID</th>
    <th></th>
  </tr>

<% @assets.each do |asset| %>
  <tr>
    <td><%= asset.name %></td>
    <td><%= asset.resizor_id %></td>
    <td><img src="<%= asset.url_for_size('c100x100') %>"></td>
    <td><%= link_to 'Destroy', asset, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Asset', new_asset_path %>
